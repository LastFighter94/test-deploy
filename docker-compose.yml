version: "3.8"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80"  # фронт отдаёт статику сразу
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://5.35.92.117:8000
