1 -

надо сделать .env для прода и локалки, чтобы docker-compose подхватывал именно определенный .env в зависимости от самого docker-compose и чтобы его можно было парсить в

async function fetchItems() { try { const res = await fetch("http://5.35.92.117:8000/items"); // const res = await fetch("http://localgost:8000/items"); items.value = await res.json(); } catch (err) { console.error("Ошибка при запросе:", err); } на fetch

Потом сделать так чтобы на проде пересборка контейнера не ломала действующее прод отображение и функционал

(чет не получилось - zero downtime deployment - пока поизучаем то что уже сделано)

// предыдущий коммит - откат до того что работало - по zero downtime deployment  - оставим на заметочку

2 - БДшку надо подключить, - сразу подключить к Nod-е помимо FastApi - чтобы был выбор между бэками - далее микрофронты
